# zuno_call_style

Internal-only plugin (not published). See the package's `pubspec.yaml`
description and `ZunoCallStylePlugin`'s doc comment for why this exists as
a real Flutter plugin rather than app-local code.
